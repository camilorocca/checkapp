import React from "react";
import { TextField } from "./AppNameInput_styled";

const AppNameInput = () => {
  return (
    <React.Fragment>
      <TextField placeholder="Text text text" />
    </React.Fragment>
  );
};

export default AppNameInput;
